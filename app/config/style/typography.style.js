// modules
import { size } from '../../utilities';

/**
 * @file Typography constants.
 * @module typography_config
 * @author Lexus Drumgold <lex@lexusdrumgold.design>
 */

// typography
const FONT_XXXXL = size(90);
const FONT_XXXL = size(45);
const FONT_XXL = size(24);
const FONT_XL = size(22);
const FONT_LARGE = size(20);
const FONT_MEDIUM = size(18);
const FONT_SMALL = size(16);
const FONT_XSMALL = size(14);
const FONT_XXSMALL = size(12);
const ICON_SIZE = FONT_XXL;

export {
  FONT_XXXXL,
  FONT_XXXL,
  FONT_XXL,
  FONT_XL,
  FONT_LARGE,
  FONT_MEDIUM,
  FONT_SMALL,
  FONT_XSMALL,
  FONT_XXSMALL,
  ICON_SIZE,
};
